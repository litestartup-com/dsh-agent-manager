# AGENTS.md —— 本仓库开发纪律（2026-09-06 拍板，对后续所有工作生效）

## 验证纪律（B 清单）

1. **上游行为先查源码**：动 DSH / pnpm / docker 等上游行为前，先读其源码或文档拿到证据，
   结论记入 `notes/DSH-FACTS.md`（事实卡）；禁止凭经验推测后推给用户当实验场。
2. **没有复现环境的修复不推**：涉及容器/部署的改动，先在本机 Docker 或 CI 的
   compose E2E job 里复现与验证；拿用户生产环境当实验场 = 事故。
3. **每个 bug 先写失败回归测试再修**：先写一个断言该 bug 的测试（跑一次证明它红），
   修代码让它转绿 —— 测试真的抓住了 bug 才算数。
4. 发布评审必须覆盖部署链（裸机 + 容器两条路径），不只审代码本身。

## 架构收敛（A 清单）

1. **真相源唯一**：`manager.config.yaml` + `.env` 是唯一手改入口；DB registry、容器、
   `fleet.md`、令牌文件等一律是**派生品，禁止手改**——改动只能回到真相源，由对账收敛。
2. **对账单一化**：DB/容器/fleet/审计四路同步收敛进一个 reconcile 入口
   （boot + 变更事件 + 定时）；禁止在多处散落形态分支（FK 事故 = 分支漏抄的产物）。
3. **节点零配置知识**：节点只读自己的 DSH_HOME 与工作区里 manager 下发的 fleet.md；
   地址只走环境变量（`$MANAGER_URL`）与预置文件，绝不写死 IP/域名。
4. **令牌落点**：`$HOME/.brain-auth`（0600）——节点用户目录，不进工作区、不进 git、
   不随工作区流动；容器由 entrypoint 从环境变量派生，裸机由 manager 写入。
5. **共享目录边界**：NFS/共享盘只放「数据类共享」（共享笔记库/素材）；约定、配置、
   密钥一律走「派生下发」（逻辑共享、物理本地），多机版经 node-agent 转发同一份。
